// import { PUBLIC_CMU_WP_REST_ENDPOINT } from "$env/static/public";

// export async function load({ fetch }) {
//     const response = await fetch(PUBLIC_CMU_WP_REST_ENDPOINT + "/posts?per_page=12");

//     return {
//         posts: response.json(),
//     };
// }
export const prerender = true;
